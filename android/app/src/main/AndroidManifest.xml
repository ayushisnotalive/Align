<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet only — no camera or storage permissions declared here.
         Those are requested at runtime, per-feature, in later phases. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Phase 2: location gate. App requires location for feed + set_location(). -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".AlignApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Align"
        android:networkSecurityConfig="@xml/network_security_config">

        <!--
            SECURITY NOTES (AGENTS.md §3):
            · allowBackup=false prevents Android Auto-Backup from capturing the
              session token. dataExtractionRules will be added in task 25 for
              fine-grained Android 12+ control.
            · networkSecurityConfig enforces HTTPS-only; no cleartext traffic.
            · FLAG_SECURE on verification/selfie screens is set per-Activity in code.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
